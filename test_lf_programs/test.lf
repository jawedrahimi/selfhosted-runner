target C {
  timeout: 1 sec
}

main reactor {
  reaction(startup) {= // This reaction will run once at startup
    printf("LF CI test: PASS\n"); 
    exit(0); // Exit with status 0 to indicate success otherwise it fails with a timeout and exits with status 1
  =}
}
